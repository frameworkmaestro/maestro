/*
 * Exemplo de código javascript no nivel da aplicação, que pode ser chamado dinamicamente pelas views.
 * (ver modules/controles/views/actions/formJavascript.xml)
 */

function dummySrc(msg) {
    console.log('DummySrc: ' + msg);
};
